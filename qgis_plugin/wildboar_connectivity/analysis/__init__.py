# Marks this folder as a Python package. Holds the actual connectivity
# analysis code: the background QgsTask (connectivity_task.py), the
# Circuitscape.jl wrapper (circuitscape_jl.py), and the resistance raster
# editor used by the fence/passage drawing tools (resistance_editor.py).
