# Marks this folder as a Python package. Holds small shared helpers that
# don't belong to any single analysis step, currently the layer-tree
# utilities in layer_utils.py (adding/removing/ordering map layers).
